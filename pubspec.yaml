name: campus_map_app
description: "A new Flutter project."
publish_to: 'none' # (주의) pub.dev에 실수로 올리는 걸 막아주는 옵션

version: 1.0.0+1 # 앱 버전

environment:
  sdk: ^3.7.2 # Dart SDK 버전

# 📦 여기부터 의존성 패키지 (dependencies) 설치 부분
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8 # iOS 스타일 아이콘 사용

# 📦 여기부터 테스트, 린트(lint) 설정 (개발 전용 패키지)
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0 # 코딩 스타일 체크 도구 (좋은 코드 쓰게 도와줌)

# 📦 여기부터 Flutter 설정 (assets, fonts 등)
flutter:
  uses-material-design: true # 머티리얼 디자인 사용 (기본 Flutter 아이콘 사용 가능)

  assets: # 📷 앱에 이미지 추가할 때
    - assets/images/ # assets/images 폴더 안에 있는 모든 이미지 사용 가능

  # (추후 필요하면 여기서 폰트(fonts) 추가 가능)